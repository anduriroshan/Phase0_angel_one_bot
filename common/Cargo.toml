[package]
name = "common"
version = "0.1.0"
edition = "2021"
description = "Shared types, enums, and binary packet parser for Phase 0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2"
tracing = "0.1"
